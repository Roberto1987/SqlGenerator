Insert creator.

Insert query:

 INSERT INTO 'table' ( field_1, field_2, .... , fielf_k)VALUES
 (V(1,1),....,V(1,k))
 (V(2,1),....,V(2,k))
        .
        .
 (V(n,1),....,V(n,k))

 Main elements: Table, (fields_1,....field_k)


 Feature:
  1. Automatic detection of the column (through header)

USE CASES.

    Use case 1: create a simple list of insert from a csv
           1. Visualize the column available in the csv
           2. Select the column to put into the insert
           3. Write the name of the field for each column
           4. Crate a file with the sql of the insert of all columns
           5. Clean eventually the csv



